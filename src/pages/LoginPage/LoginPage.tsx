import { Authentification } from "../../features/Authentication";

const LoginPage = () => {
    return (
        <div>
            <Authentification />
        </div>
    );
};

export default LoginPage;